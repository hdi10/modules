spring:
  application:
    name: zelkulo-service
server:
  port: 9010


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8090/eureka/
    register-with-eureka: true
    fetch-registry: true
